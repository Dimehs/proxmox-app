Note to AI: This file contains the "source of truth" for the Proxmox Lab Orchestrator. Refer to this before suggesting changes to network, database, or API logic.🚀 Project OverviewA custom Python/FastAPI orchestrator running in an Ubuntu LXC (10.19.100.110) to automate student lab deployments across a 3-node Proxmox cluster.🌐 Network Architecture (HPE L3 Switch)Management/Native VLAN (VLAN 1): 10.19.100.0/24 (Hosts & Admin App).Infrastructure Services (VLAN 20): 10.20.0.0/24 (Centralized DNS).Student Labs (VLAN 50 - 120):Logic: $VLAN = 50 + TableID$Subnet: 10.10.VLAN.0/24Gateway: 10.10.VLAN.1 (HPE Switch SVI)🏗️ Lab "Pod" SpecificationsEach table deployment consists of:Attacker (Kali VM): Static IP 10.10.VLAN.100 (Cloud-Init).Target 1 (LXC): Static IP 10.10.VLAN.101.Target 2 (LXC): Static IP 10.10.VLAN.102.DHCP Pool: Range .150 to .200 for additional student devices.🗄️ Database Schema (SQLite)templates: Maps Proxmox VMIDs to roles (attacker, target).tables: Tracks table_number and its assigned vlan_id.deployed_resources: Tracks active VMIDs, the node they live on, and their type (QEMU/LXC).🛠️ Tech Stack & PathsLanguage: Python 3.11+ (FastAPI)Environment: /opt/proxmox-orchestrator/venvStorage: All disks must go to the "Data" storage pool (RAID 10).Node Balancing: Round-robin placement ($TableID \pmod 3$).⚠️ Coding RulesNever hardcode secrets: Use os.getenv from the .env file.PVE Connection: Always use the get_px() helper to handle the 3-node cluster failover.Static IPs: For VMs, use ipconfig0 in Cloud-Init. For LXC, use the net0 parameter.